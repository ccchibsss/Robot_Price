# ============================================================
# ЗАВИСИМОСТИ ДЛЯ РОБОТА ПО ЗАГРУЗКЕ И АНАЛИЗУ ПРАЙСОВ v13.0
# ============================================================

# Основной веб-интерфейс и дашборды
streamlit>=1.20.0
plotly>=5.11.0

# Обработка и анализ данных
pandas>=1.5.0
numpy>=1.23.0

# HTTP-запросы и работа с API
requests>=2.28.0

# Парсинг HTML/XML (для BeautifulSoup)
beautifulsoup4>=4.11.0
lxml>=4.9.0

# Работа с Excel и другими табличными форматами
openpyxl>=3.0.10
xlrd>=2.0.1
odfpy>=1.4.1

# Google Sheets API и авторизация
gspread>=5.5.0
oauth2client>=4.1.3
google-api-python-client>=2.70.0
google-auth>=2.15.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0

# Определение кодировок и MIME-типов
chardet>=5.0.0
python-magic>=0.4.27

# Работа с архивами (ZIP, TAR, 7z, RAR)
py7zr>=0.20.0
rarfile>=4.0              # требует установки unrar в системе (см. инструкцию)

# Утилиты для работы с датами и временем
python-dateutil>=2.8.2
pytz>=2022.6

# Дополнительные утилиты (опционально, но могут пригодиться)
pyyaml>=6.0               # для работы с YAML (не используется, но безопасно)
defusedxml>=0.7.1         # безопасный парсинг XML

# Для уведомлений в Telegram (опционально – можно удалить, если не планируется использовать)
python-telegram-bot>=20.0

# Для параллельной обработки больших данных (опционально)
joblib>=1.2.0
dask>=2022.10.0

# Для ускоренной работы с CSV (опционально)
fastcsv>=0.1.0

# ============================================================
# КОМАНДА ДЛЯ УСТАНОВКИ ВСЕХ ЗАВИСИМОСТЕЙ
# ============================================================
# pip install -r requirements.txt
#
# ПРИМЕЧАНИЕ: Для работы python-magic на Windows может потребоваться
# установка DLL-библиотеки, на Linux/macOS – libmagic.
# Для RAR-архивов нужен установленный unrar (в системе).
# ============================================================
