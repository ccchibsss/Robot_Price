# ============================================================
# ЗАВИСИМОСТИ ДЛЯ РОБОТА ПО ЗАГРУЗКЕ И АНАЛИЗУ ПРАЙСОВ v13.0
# БЕЗ ПРОБЛЕМНЫХ ПАКЕТОВ (fastcsv, dask, joblib и др.)
# ============================================================

# Веб-интерфейс и визуализация
streamlit>=1.20.0
plotly>=5.11.0

# Обработка и анализ данных
pandas>=1.5.0
numpy>=1.23.0

# HTTP-запросы и работа с API
requests>=2.28.0

# Парсинг HTML/XML (для BeautifulSoup)
beautifulsoup4>=4.11.0
lxml>=4.9.0

# Работа с Excel и другими табличными форматами
openpyxl>=3.0.10
xlrd>=2.0.1
odfpy>=1.4.1

# Google Sheets API и авторизация
gspread>=5.5.0
oauth2client>=4.1.3
google-api-python-client>=2.70.0
google-auth>=2.15.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0

# Определение кодировок и MIME-типов
chardet>=5.0.0
python-magic>=0.4.27

# Работа с архивами (ZIP, TAR, 7z, RAR)
py7zr>=0.20.0
rarfile>=4.0              # требует установки unrar в системе

# Утилиты для работы с датами и временем
python-dateutil>=2.8.2
pytz>=2022.6

# Для работы с YAML (необязательно, но безопасно)
pyyaml>=6.0

# Безопасный парсинг XML
defusedxml>=0.7.1

# Для уведомлений в Telegram (опционально)
python-telegram-bot>=20.0

# ============================================================
# УДАЛЕНЫ ПРОБЛЕМНЫЕ ПАКЕТЫ:
# - fastcsv (не собирается на Python 3.14)
# - dask (не обязателен, можно установить при необходимости)
# - joblib (не обязателен)
# - pyexcel, pyexcel-xls, pyexcel-xlsx (не обязательны, openpyxl/xlrd достаточно)
# ============================================================

# КОМАНДА ДЛЯ УСТАНОВКИ:
# pip install -r requirements.txt
